namespace ThornClient.Core;

/// <summary>
/// The type of a setting. Mostly used for the UI.
/// </summary>
internal enum SettingType {
    Bind,
    Bool,
    Color,
    EnemyList,
    Enum,
    Float,
    Int,
    Text,
    Unsupported,
}
