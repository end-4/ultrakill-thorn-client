namespace ThornClient.Core;

/// <summary>
/// The type of a setting. Mostly used for the UI.
/// </summary>
public enum SettingType {
    Bind,
    Bool,
    Color,
    EnemyList,
    Enum,
    Float,
    Int,
    Text,
    Unsupported,
}
